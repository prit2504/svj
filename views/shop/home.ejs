<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Shree Vagheswari Jewellers</title>
  <link
    href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Lora:wght@400;500&display=swap"
    rel="stylesheet" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      font-family: 'Lora', serif;
    }

    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
      font-family: 'Playfair Display', serif;
    }

    .hover-scale {
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .hover-scale:hover {
      transform: translateY(-6px);
      box-shadow: 0 15px 25px rgba(0, 0, 0, 0.12);
    }

    .gradient-btn {
      background: linear-gradient(135deg, #B76E79, #E3C396);
    }

    .hero-bg {
      background: url('/images/hero.jpg') center/cover no-repeat;
    }

    .product-card {
      background: linear-gradient(145deg, #fff8f0, #fff1f3);
      border-radius: 1rem;
      transition: all 0.3s;
    }

    .product-card:hover {
      transform: translateY(-6px);
      box-shadow: 0 15px 25px rgba(0, 0, 0, 0.12);
    }
  </style>
</head>

<body class="bg-gradient-to-br from-[#fffdf9] via-[#fdf7f2] to-[#fffaf5] text-gray-800">

  <div class="flex flex-col justify-center">
    <nav class="bg-white shadow-lg sticky top-0 z-50">
      <div class="max-w-7xl mx-auto px-4 py-4 flex items-center justify-between">
        <div class="flex items-center space-x-3">
          <img src="/images/logo.png" alt="Logo" class="h-12 w-12 object-contain" />
          <div class="text-2xl md:text-3xl font-bold text-[#B76E79]">Shree Vagheswari Jewellers</div>
        </div>
        <div class="space-x-4 hidden md:flex">
          <a href="#" class="text-gray-700 hover:text-[#E3C396] font-medium transition">Home</a>
          <a href="#categories" class="text-gray-700 hover:text-[#E3C396] font-medium transition">Collections</a>
          <a href="#footer" class="text-gray-700 hover:text-[#E3C396] font-medium transition">Contact</a>
        </div>
      </div>
    </nav>

    <!-- Mobile Search -->
    <!-- <div
    class="sm:hidden flex items-center mx-4 px-3 py-2 rounded-lg shadow-md cursor-pointer hover:bg-[#f9f5f0] transition duration-200 mt-4 mb-4 border border-[#E3C396]/40"
    onclick="window.location.href='/search'">
    <svg class="w-5 h-5 text-[#B76E79]" fill="none" stroke="currentColor" viewBox="0 0 24 24"
      xmlns="http://www.w3.org/2000/svg">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
        d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
    </svg>
    <span class="ml-3 text-[#7A6C5D] text-sm">Search jewellery</span>
  </div> -->

    <!-- Offer Banner -->


    <!-- Hero Section -->
    <section
      class="mx-4 my-4 bg-gradient-to-r from-[#fffaf5] to-[#fdf3e7] border border-red-500 rounded-2xl p-10 shadow-xl text-center">
      <div class="relative z-10 text-white px-4 animate-fadeIn" style="animation: fadeIn 0.7s ease-out;">
        <h1 class="text-4xl sm:text-6xl font-serif font-bold text-[#B76E79] mb-4 leading-snug">Welcome to <br />Shree
          Vagheswari Jewellers</h1>
        <p class="text-sm sm:text-lg text-gray-600 mb-6 max-w-2xl mx-auto">Explore our handcrafted jewellery
          collections,
          blending tradition and modern design for every special occasion.</p>
        <a href="#categories"
          class="bg-gradient-to-r from-[#B76E79] to-[#E3C396] text-white px-8 py-3 rounded-full font-medium shadow-md hover:opacity-90 transition">Shop
          Now</a>
      </div>
    </section>





    <!-- Search Section -->
    <section class="max-w-7xl mx-auto px-4 py-8 flex flex-col md:flex-row gap-4 items-center justify-center">
      <select id="categorySelect"
        class="w-full md:w-1/4 border border-pink-200 rounded-lg px-4 py-2 bg-white text-gray-700 focus:outline-none focus:ring-2 focus:ring-[#E3C396] transition">
        <option value="All">All Categories</option>
        <% categories.forEach(cat=> { %>
          <option value="<%= cat %>">
            <%= cat %>
          </option>
          <% }) %>
      </select>
      <input type="text" id="search" placeholder="Search for treasures..."
        class="w-full md:flex-1 border border-pink-200 rounded-lg px-4 py-2 bg-white text-gray-700 focus:outline-none focus:ring-2 focus:ring-[#E3C396] transition" />
      <button id="searchButton"
        class="bg-gradient-to-r from-[#B76E79] to-[#E3C396] text-white px-6 py-2 rounded-lg font-medium shadow-md hover:opacity-90 transition">Search</button>
    </section>
    <!-- Offer Banner -->
    <!-- Offer Banner -->
    <section class="mx-4 my-1">
      <div class="max-w-6xl mx-auto">
        <div class="aspect-w-16 aspect-h-9 rounded-2xl overflow-hidden shadow-lg">

          <!-- Mobile Image -->
          <img src="/images/offer1.webp" alt="Special Offer Mobile"
            class="w-full h-full object-cover block sm:hidden" />

          <!-- Desktop Image -->
          <img src="/images/offer1.1.webp" alt="Special Offer Desktop"
            class="w-full h-full object-cover hidden sm:block" />

        </div>
      </div>
    </section>

    <!-- Categories Section -->
    <section id="categories" class="max-w-7xl mx-auto px-4 py-10">
      <h2 class="text-2xl font-bold text-center text-yellow-700 mb-8">Explore Categories</h2>
      <div class="grid grid-cols-3 sm:grid-cols-5 md:grid-cols-6 lg:grid-cols-9 gap-3">
        <% const categoryImages={ "Gents Chain" : "/assets/categories/gents_chain.jpg" , "Gents Bracelets"
          : "/assets/categories/gents_bracelet.jpg" , "Gents Kada" : "/assets/categories/gents_kada.jpg"
          , "Rudraksh Mala/Bracelets" : "/assets/categories/rudraksh_mala.jpg" , "Pendals"
          : "/assets/categories/pandant.jpg" , "Gente Kadi" : "/assets/categories/gents_kadi.jpg" , "Ladies Chain"
          : "/assets/categories/ladies_chain.jpg" , "Ladies Bracelets" : "/assets/categories/ladies_bracelates.jpg"
          , "Ladies Kada" : "/assets/categories/ladies_kada.jpg" , "Golden Dokiya" : "/assets/categories/dokiya.jpg"
          , "Dokiya Mangalsutra" : "/assets/categories/dokiya_mangalsutra.jpg" , "Long Mangalsutra"
          : "/assets/categories/long_mangalsutra.jpg" , "Half Set" : "/assets/categories/half_nacklaceset.jpg"
          , "Long Set ( Rani Har )" : "/assets/categories/rani_haar_neckeless.jpg" , "Bangles"
          : "/assets/categories/bangles.jpg" , "Chudi Bangles" : "/assets/categories/chudi_bangles.jpg" , "Fancy Mala"
          : "/assets/categories/fancy_mala.jpg" }; %>
          <% categories.forEach(cat=> { %>
            <a href="/category/<%= encodeURIComponent(cat) %>"
              class="m-2 border border-red-600 w-[100px] h-[100px] sm:w-[120px] sm:h-[120px] group relative rounded-xl overflow-hidden shadow-md hover:shadow-xl transition duration-300">
              <img src="<%= categoryImages[cat] %>" alt="<%= cat %>"
                class="w-full object-cover transform group-hover:scale-110 transition duration-500" />
              <div
                class="absolute inset-0 bg-black/40 group-hover:bg-black/60 transition flex items-center justify-center">
                <span class="text-white font-semibold text-sm sm:text-base px-2 text-center">
                  <%= cat %>
                </span>
              </div>
            </a>
            <% }) %>

      </div>
    </section>

    <!-- Loader -->
    <div id="loader" class="hidden flex justify-center py-6">
      <div class="w-12 h-12 border-4 border-[#E3C396] border-dashed rounded-full animate-spin"></div>
    </div>

    <!-- Product Grid -->
    <section id="products" class="border-gray-200 border-2 mx-2 sm:mx-6 rounded-lg px-4 py-8 
         grid grid-cols-2 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 w-[95%]">

      <!-- Dynamic Products Rendered Here -->

      <!-- Static How to Use Cards in Flexbox -->
      <div class="col-span-full flex justify-evenly gap-4">
        <div class="product-card border p-2 flex flex-col items-center">
          <img src="/images/htu1.webp" alt="How to use" loading="lazy"
            class="rounded-lg shadow-md hover:scale-105 transition w-full max-w-[200px] object-cover">
        </div>

        <div class="product-card border p-2 flex flex-col items-center">
          <img src="/images/htu2.webp" alt="How to use" loading="lazy"
            class="rounded-lg shadow-md hover:scale-105 transition w-full max-w-[200px] object-cover">
        </div>
      </div>
    </section>




    <!-- Pagination -->
    <div id="pagination" class="flex justify-center mt-8 gap-3"></div>

    <!-- Footer -->
    <footer id="footer" class="bg-[#faf7f2] border-t border-[#E3C396]/30 text-[#6B5B4D] mt-12 p-8 text-sm">
      <div class="max-w-6xl mx-auto grid grid-cols-2 sm:grid-cols-4 gap-6">
        <div>
          <h3 class="text-[#B76E79] font-semibold mb-3">Address</h3>
          <ul class="space-y-2">
            <li>
              <a href="/shop-location" class="hover:underline text-gray-700">
                Shamal Bechor Pole, Vithhal Mandir Lane, B/h Bank Of Baroda, Mandvi, Vadodara. 390001
              </a>
            </li>
          </ul>
        </div>

        <div>
          <h3 class="text-[#B76E79] font-semibold mb-3">Contact Us | Help</h3>
          <ul class="space-y-2">
            <li class="hover:text-[#E3C396] cursor-pointer">
              <a href="https://wa.me/message/YYDUQB5Y2PCTC1" target="_blank">WhatsApp</a>
            </li>
          </ul>
        </div>
        <div>
          <h3 class="text-[#B76E79] font-semibold mb-3">Follow Us</h3>
          <ul class="space-y-2">
            <li class="hover:text-[#E3C396] cursor-pointer">
              <a href="https://www.instagram.com/1gram_vagheswari_jewellers" target="_blank">Instagram</a>
            </li>
            <li class="hover:text-[#E3C396] cursor-pointer">
              <a href="https://chat.whatsapp.com/GEScwwYBCmu7h8vUasRMdL" target="_blank">WhatsApp</a>
            </li>
          </ul>
        </div>
      </div>
      <p class="text-center mt-8 text-xs text-gray-500">© 2025 Shree Vagheswari Jewellers. All rights reserved.</p>
    </footer>

    <!-- Scroll to Top -->
    <button id="scrollTop"
      class="hidden fixed bottom-6 right-6 gradient-btn p-3 rounded-full shadow-lg hover:scale-110 transition duration-300 z-50"
      aria-label="Scroll to top">
      <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"
        xmlns="http://www.w3.org/2000/svg">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 10l7-7m0 0l7 7m-7-7v18"></path>
      </svg>
    </button>
  </div>


  <script>
    // Scroll to Top Logic
    const scrollTopBtn = document.getElementById('scrollTop');
    window.addEventListener('scroll', () => {
      scrollTopBtn.classList.toggle('hidden', window.scrollY <= 300);
    });
    scrollTopBtn.addEventListener('click', () => {
      window.scrollTo({ top: 0, behavior: 'smooth' });
    });

    // Product Fetch Logic
    const productsContainer = document.getElementById('products');
    const categorySelect = document.getElementById('categorySelect');
    const searchInput = document.getElementById('search');
    const searchButton = document.getElementById('searchButton');
    const loader = document.getElementById('loader');
    const paginationContainer = document.getElementById('pagination');
    let currentPage = 1;
    const limit = 8;

    async function fetchProducts() {
      loader.classList.remove('hidden');
      const category = categorySelect.value;
      const search = searchInput.value;
      const res = await fetch(`/api/products?category=${category}&search=${search}&page=${currentPage}&limit=${limit}`);
      const data = await res.json();
      loader.classList.add('hidden');

      productsContainer.innerHTML = '';
      if (data.products.length === 0) {
        productsContainer.innerHTML = '<p class="text-center text-gray-600 col-span-full">No treasures found.</p>';
      } else {
        data.products.forEach(product => {
          const div = document.createElement('a');
          div.href = `/product/${product._id}`;
          div.className = `
  group product-card flex flex-col overflow-hidden rounded-2xl shadow-md border border-[#f5e6d3] 
  bg-gradient-to-br from-[#fffdf9] via-[#fff8f2] to-[#fff3ea] hover:shadow-xl hover:-translate-y-1 
  transition-all duration-300
`;

 div.innerHTML = ` 
  <!-- Image Section -->
  <div class="relative overflow-hidden w-full aspect-square">
    <img src="${product.imageUrl}" alt="${product.title}" loading="lazy"
         class="absolute inset-0 w-full h-full object-cover transition-transform duration-500 ease-in-out group-hover:scale-105 rounded-lg">
    
    ${!product.inStock ? `
      <span class="absolute top-3 left-3 bg-red-600 text-white text-xs px-2 py-1 rounded-full shadow">Out of Stock</span>
    ` : `
      <span class="absolute top-3 left-3 bg-green-600 text-white text-xs px-2 py-1 rounded-full shadow">Available</span>
    `}

    <div class="absolute inset-0 bg-black/0 group-hover:bg-black/20 transition"></div>
  </div>

  <!-- Content Section -->
  <div class="p-4 flex flex-col flex-1 font-sans">
    <h3 class="font-semibold text-base sm:text-lg text-gray-800 line-clamp-1 mb-1">
      ${product.title}
    </h3>

    ${product.description ? `
      <p class="text-gray-600 text-sm mb-2 line-clamp-2">
        ${product.description}
      </p>
    ` : ``}

    <div class="footer border-t border-gray-200 pt-3 mt-2 text-sm space-y-1">
      <!-- MRP -->
      <div class="text-gray-500 line-through">
        MRP : ₹${product.mrp || '---'}
      </div>

      <!-- Selling Price -->
      <div class="text-gray-800 font-medium">
        Price : ₹${product.price || '---'} 
        <span class="text-green-600 font-semibold">
          (${product.mrp && product.price ? Math.round(((product.mrp - product.price) / product.mrp) * 100) : 0}% OFF)
        </span>
      </div>

      <!-- Extra Discount -->
      <div class="text-yellow-600 font-medium">
        Extra 10% OFF applied
      </div>

      <!-- Final Price -->
      <div class="text-green-700 font-bold">
        Final Price : ₹${product.price ? Math.round(product.price - (product.price * 0.10)) : '---'}

      </div>

      <!-- Button -->
      <button class="mt-3 bg-[#B76E79] text-white px-4 py-1.5 rounded-md shadow hover:opacity-90 transition">
        View
      </button>
    </div>
  </div>
`;


          productsContainer.appendChild(div);
        });
      }

      paginationContainer.innerHTML = '';
      for (let i = 1; i <= data.pages; i++) {
        const btn = document.createElement('button');
        btn.textContent = i;
        btn.className = `px-4 py-2 rounded-full ${i === data.page ? 'bg-yellow-500 text-white shadow-lg' : 'bg-pink-100 text-gray-700 hover:bg-yellow-200'} transition`;
        btn.onclick = () => { currentPage = i; fetchProducts(); };
        paginationContainer.appendChild(btn);
      }

      // Scroll to products section
      productsContainer.scrollIntoView({ behavior: 'smooth' });
    }

    searchButton.addEventListener('click', () => { currentPage = 1; fetchProducts(); });
  </script>

</body>

</html>