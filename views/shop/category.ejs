<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= category %> | Jewellery Shop
  </title>
  <style>
    /* Global */
    body {
      background-color: #f9fafb;
      color: #1f2937;
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    /* Navbar */
    nav {
      background-color: #fff;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
      position: sticky;
      top: 0;
      z-index: 50;
    }

    nav .container {
      max-width: 1280px;
      margin: 0 auto;
      padding: 12px 16px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      flex-wrap: wrap;
    }

    nav img {
      height: 40px;
      width: auto;
    }

    nav .brand {
      font-size: 20px;
      font-weight: bold;
      color: #B76E79;
      margin-left: 8px;
    }

    /* Header */
    section.header {
      max-width: 1280px;
      margin: 0 auto;
      padding: 24px 16px;
      position: sticky;
      top: 60px;
      /* stick below navbar */
      background: #f9fafb;
      z-index: 40;
    }

    section.header h2 {
      font-size: 26px;
      font-weight: bold;
      margin-bottom: 16px;
    }

    /* Sticky Back Button */
    .back-button {
      background-color: #e5e7eb;
      color: #374151;
      padding: 8px 16px;
      border-radius: 6px;
      transition: background-color 0.3s, transform 0.2s;
      font-size: 14px;
      font-weight: 500;
      cursor: pointer;
      border: none;
    }

    .back-button:hover {
      background-color: #d1d5db;
      transform: scale(1.05);
    }

    /* Products Grid */
    .products {
      max-width: 1280px;
      margin: 0 auto;
      padding: 0 16px 40px;
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      /* default: 2 per row (mobile) */
      gap: 16px;
    }

    /* Card */
    .product-card {
      border: 1px solid #ddd;
      border-radius: 8px;
      overflow: hidden;
      background: #fff;
      text-decoration: none;
      color: inherit;
      display: flex;
      flex-direction: column;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .product-card:hover {
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.2);
      transform: translateY(-4px);
    }

    .product-card img {
      width: 100%;
      aspect-ratio: 1 / 1;
      object-fit: cover;
      transition: transform 0.5s ease;
      display: block;
    }

    .product-card:hover img {
      transform: scale(1.05);
    }

    .product-card .relative {
      position: relative;
      overflow: hidden;
    }

    .product-card .status {
      position: absolute;
      top: 12px;
      left: 12px;
      padding: 4px 8px;
      font-size: 12px;
      border-radius: 999px;
      color: #fff;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
    }

    .out-of-stock {
      background-color: #dc2626;
    }

    .available {
      background-color: #16a34a;
    }

    .overlay {
      position: absolute;
      inset: 0;
      background-color: rgba(0, 0, 0, 0);
      transition: background-color 0.3s;
    }

    .product-card:hover .overlay {
      background-color: rgba(0, 0, 0, 0.2);
    }

    .product-card .content {
      padding: 16px;
      display: flex;
      flex-direction: column;
      flex: 1;
    }

    .product-card h3 {
      font-size: 16px;
      font-weight: 600;
      margin: 0;
      color: #1f2937;
      transition: color 0.3s;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .product-card:hover h3 {
      color: #B76E79;
    }

    .product-card p {
      font-size: 14px;
      color: #6b7280;
      margin-top: 4px;
    }

    .product-card .footer {
      margin-top: 10px;
      display: flex;
      flex-direction: column;
      align-items: start;
      gap: 6px;
      font-size: 14px;
    }

    .mrp {
      color: #777;
      text-decoration: line-through;
    }

    .price {
      color: #B76E79;
      font-weight: bold;
    }

    .price span {
      color: green;
      font-weight: bold;
      margin-left: 4px;
    }

    .extra {
      color: #d97706;
      font-weight: 600;
    }

    .final {
      color: #059669;
      font-weight: bold;
      font-size: 1.05em;
    }

    .final span {
      color: #059669;
    }

    .product-card button {
      background: linear-gradient(to right, #B76E79, #E3C396);
      color: white;
      font-size: 14px;
      padding: 6px 16px;
      border: none;
      border-radius: 999px;
      cursor: pointer;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
      transition: all 0.3s;
      margin-top: 8px;
    }

    .product-card button:hover {
      opacity: 0.9;
      transform: scale(1.05);
    }

    /* Pagination */
    .pagination {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 8px;
      margin-bottom: 40px;
      flex-wrap: wrap;
    }

    .pagination a {
      padding: 8px 16px;
      background-color: #e5e7eb;
      color: #374151;
      border-radius: 6px;
      transition: background-color 0.3s;
    }

    .pagination a:hover {
      background-color: #d1d5db;
    }

    .pagination span {
      color: #374151;
      font-size: 14px;
    }

    /* ---------------- Responsive Breakpoints ---------------- */
    @media (min-width: 640px) {
      .products {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (min-width: 768px) {
      .products {
        grid-template-columns: repeat(3, 1fr);
      }
    }

    @media (min-width: 1024px) {
      .products {
        grid-template-columns: repeat(4, 1fr);
      }
    }

    @media (max-width: 480px) {
      section.header h2 {
        font-size: 22px;
      }

      nav .brand {
        font-size: 18px;
      }

      .product-card h3 {
        font-size: 14px;
      }

      .price {
        font-size: 15px;
      }
    }
  </style>
</head>

<body>

  <!-- Navbar -->
  <nav>
    <div class="container">
      <a href="/" style="display: flex; align-items: center;">
        <img src="/images/logo.png" alt="Logo">
        <span class="brand">Shree Vagheswari Jewellers</span>
      </a>
    </div>
  </nav>

  <!-- Header -->
  <section class="header">
    <h2>
      <%= category %> Collection
    </h2>
    <button type="button" onclick="window.history.back()" class="back-button">
      ← Back
    </button>
  </section>

  <!-- Products Grid -->
  <section class="products">
    <% if (products.length===0) { %>
      <p style="text-align: center; color: #6b7280; grid-column: 1 / -1;">
        No products found in <%= category %>.
      </p>
      <% } else { %>
        <% products.forEach(product=> { %>
          <a href="/product/<%= product._id %>" class="product-card">
            <div class="relative">
              <img src="<%= product.imageUrl %>" alt="<%= product.title %>" loading="lazy"/>
              <% if (!product.inStock) { %>
                <span class="status out-of-stock">Out of Stock</span>
                <% } else { %>
                  <span class="status available">Available</span>
                  <% } %>
                    <div class="overlay"></div>
            </div>

            <div class="content">
              <h3>
                <%= product.title %>
              </h3>
                <% if (product.description) { %>
                  <p class="text-gray-700">
                    <%= product.description %>
                  </p>
                  <% } %>

              <div class="footer">
                <% let baseDiscount=product.mrp && product.price ? Math.round(((product.mrp - product.price) /
                  product.mrp) * 100) : 0; let extraDiscountedPrice=product.price ? Math.round(product.price -
                  (product.price * 0.10)) : null; let totalDiscount=product.mrp && extraDiscountedPrice ?
                  Math.round(((product.mrp - extraDiscountedPrice) / product.mrp) * 100) : 0; %>

                  <div class="mrp">MRP : ₹<%= product.mrp || '---' %>
                  </div>
                  <div class="price">
                    Price : ₹<%= product.price || '---' %>
                      <span>(<%= baseDiscount %>% OFF)</span>
                  </div>
                  <div class="extra">Extra 10% OFF applied</div>
                  <div class="final">
                    Final Price : ₹<%= extraDiscountedPrice || '---' %>

                  </div>

                  <button>View</button>
              </div>
            </div>
          </a>
          <% }) %>
            <% } %>
  </section>

  <!-- Pagination -->
  <% if (totalPages> 1) { %>
    <div class="pagination">
      <% if (currentPage> 1) { %>
        <a href="?page=<%= currentPage - 1 %>">Prev</a>
        <% } %>
          <span>Page <%= currentPage %> of <%= totalPages %></span>
          <% if (currentPage < totalPages) { %>
            <a href="?page=<%= currentPage + 1 %>">Next</a>
            <% } %>
    </div>
    <% } %>

</body>

</html>